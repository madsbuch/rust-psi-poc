# Rust Private Set Intersection Proof of Concept

This is a small proof of concept and educational example on how to do private
set intersection in Rust.

To run:

```
rustup override set nightly
cargo run --release
```
